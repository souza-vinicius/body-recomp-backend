import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bodyrecomp.app',
  appName: 'Body Recomp',
  webDir: 'out',
  bundledWebRuntime: false
};

export default config;
